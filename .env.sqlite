# SQLite Configuration (Current System)
# This configuration is for running the application with SQLite database
DB_TYPE=sqlite
SQLITE_PATH=cvd.db
FLASK_ENV=production
SECRET_KEY=your-secret-key-change-in-production
FLASK_PORT=5000

# Optional AI features (leave empty to disable)
ANTHROPIC_API_KEY=